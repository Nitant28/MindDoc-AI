# Context Builder: select, deduplicate, limit tokens

def build_context(docs, token_limit=1500):
    """
    Build context from docs, limit tokens, remove redundancy.
    """
    # Placeholder: return docs as-is
    return docs
